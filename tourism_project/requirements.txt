# Dependencies for the GitHub Actions pipeline jobs (register -> prep -> train).
# Versions are pinned so every pipeline run is reproducible and matches the
# deployment requirements.txt used by the Streamlit app.
pandas==2.2.2
scikit-learn==1.5.1
xgboost==2.1.1
mlflow==3.0.1
joblib==1.4.2
